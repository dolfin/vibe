kind: vibe.app/v1
id: com.example.postgres-bookmarks
name: Bookmarks (PostgreSQL)
version: "1.0.0"

services:
  - name: web
    image: node:20-alpine
    command: ["sh", "start.sh"]
    ports:
      - container: 3000
    mounts:
      - source: data
        target: /data

state:
  volumes:
    - name: data
      consistency: generic

security:
  network: true

publisher:
  name: Vibe Examples
